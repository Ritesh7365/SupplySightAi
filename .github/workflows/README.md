# .github/workflows

## Purpose

GitHub Actions workflow definitions for CI/CD.

## Contents

Placeholder workflow stubs (see `ci.yml` scaffold). Expand when application code exists.

## Future Implementation

- Lint + unit tests for frontend and backend
- Docker build and push
- Optional deploy jobs with environment protection
