# backend/app/api

## Purpose

API package: router aggregation and versioning.

## Contents

`routes/` for endpoint modules.

## Future Implementation

- `api_router` including all domain routers under `/api/v1`
