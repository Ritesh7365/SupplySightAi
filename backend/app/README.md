# backend/app

## Purpose

Python package root for the FastAPI application.

## Contents

Subpackages: `api`, `models`, `schemas`, `services`, `core`, `database`, `middleware`, `utils`, `config`, `ml`.

## Future Implementation

- `__init__.py` and `main.py` application entry
- Lifespan hooks for DB and model loading
