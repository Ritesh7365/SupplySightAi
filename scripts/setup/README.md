# scripts/setup

## Purpose

One-time and recurring local environment setup (venv, node install, env copy, pre-commit).

## Contents

Placeholder scripts only in later phases.

## Future Implementation

- `bootstrap.sh` / `bootstrap.ps1`
- Tooling version checks (Node, Python, Docker)
