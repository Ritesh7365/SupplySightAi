# Placeholder normalized tables

`warehouses.csv`, `inventory.csv`, and `vendors.csv` contain **headers only** matching `database/schema/` DDL.

The DataCo extract has no warehouse, inventory-quantity, or vendor identifiers. Populate these files in a later phase using synthetic or external WMS / ERP / SRM datasets.
