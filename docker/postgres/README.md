# docker/postgres

## Purpose

PostgreSQL initialization scripts and configuration overlays for local/dev containers.

## Contents

Placeholder. Mount path reserved in `docker-compose.yml` comments.

## Future Implementation

- `init` SQL for extensions (e.g., uuid-ossp)
- Optional tuned `postgresql.conf` for analytics workloads
